export type ProjectType = {
  id: number;
  name: string;
  description?: string;
};

export type ProjectFormValuesType = {
  name: string;
  description?: string;
};

export type ProjectAPIRequestType = {
  name: string;
  description?: string;
};
